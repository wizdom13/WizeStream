name: Apply PR 387 viewport race fix

on:
  push:
    branches:
      - fix-fullscreen-exit-aspect-ratio

permissions:
  contents: write

jobs:
  patch:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          ref: fix-fullscreen-exit-aspect-ratio

      - name: Apply patch
        run: python3 .github/pr387_viewport_patch.py

      - name: Commit patch
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add app/src/main/java/org/schabi/newpipe/fragments/detail/VideoDetailFragment.java app/src/test/java/org/schabi/newpipe/fragments/detail/VideoDetailOrientationHandlingTest.java .github/pr387_viewport_patch.py .github/workflows/temporary-pr387-viewport-race-fix.yml
          git commit -m "Remove fullscreen viewport timing race"
          git push origin HEAD:fix-fullscreen-exit-aspect-ratio
